# Database configuration
DB_HOST=localhost
DB_NAME=timetracking_mm
DB_USER=your_db_user
DB_PASS=your_db_password

# JWT secret - generate a random string (e.g. openssl rand -hex 32)
JWT_SECRET=change-this-to-a-random-string
